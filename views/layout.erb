<!DOCTYPE html>
    <html>
        <head>
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/1.11.8/semantic.min.css"/>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/1.11.8/semantic.min.js"></script>
            <link rel="stylesheet" type="text/css" href="/css/layout.css">
        </head>

        <body>

        <nav>
        <a href="/"><h1>Alpinists</h1></a>
        <% if session[:user_id] %>
            <h2> <%= @user_username %></h2>
            <a href="/user/<%= session[:user_id] %>/profile"><button class="ui blue button">Your Blog</button></a>
            <a href="/user/<%= session[:user_id] %>/post"><button class="ui green button">Create a post</button></a>
            <a href="/user/<%= session[:user_id] %>/settings"><button class="ui gray button">Settings</button></a>
            <a href="/signout"><button class="ui red button">Sign out</button></a>

        <% else %>
        <a href="/signin">
       <button class="ui inverted blue button">Sign In</button></a>
        <a href="/signup"><button class="ui blue button">Sign Up</button></a>
        <% end %>
        </nav>
        <div class="wrapper">
            <%= yield %>
            </div>
        </body>
        

</html>